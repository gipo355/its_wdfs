import { generatedBaseApiTypes } from './generated-base-api-types';

describe('generatedBaseApiTypes', () => {
  it('should work', () => {
    expect(generatedBaseApiTypes()).toEqual('generated-base-api-types');
  });
});
